#!/usr/bin/env sh

# TODO: enable this once we're converted to consistent formatting
# echo "🎨 Running npm run format..."
# npm run format

echo "📝 Checking for unstaged changes..."
if ! git diff --quiet; then
  echo "❌ npm run format corrected some files, or you have unstaged changes!"
  echo "Please run 'git add' on the corrected files and try committing again."
  exit 1
fi

# TODO: enable this once we're lint clean
# echo "🔍 Running npm run lint..."
# npm run lint || { echo "❌ Linting failed. Please fix errors before committing."; exit 1; }

echo "🧪 Running npm run test..."
npm run test || { echo "❌ Tests failed. Please fix them before committing."; exit 1; }

echo "✅ Pre-commit checks passed!"
